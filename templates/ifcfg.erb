DEVICE="<%= @name %>"
BOOTPROTO="none"
NM_CONTROLLED="no"
ONBOOT="<%= @onboot %>"
TYPE="InfiniBand"
IPADDR="<%= @ipaddr %>"
NETMASK="<%= @netmask %>"
<% if @gateway != 'UNSET' -%>
GATEWAY="<%= @gateway %>"
<% end -%>
CONNECTED_MODE="<%= @connected_mode %>"
<% if @mtu != 'UNSET' -%>
MTU="<%= @mtu %>"
<% end -%>
